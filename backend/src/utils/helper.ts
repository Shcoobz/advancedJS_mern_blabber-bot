import { SUCCESS } from '../constants/constants.js';

/**
 * Function to format server start message with dynamic port
 */
export function getServerStartMessage() {
  const serverStart = SUCCESS.SERVER.START;

  return serverStart;
}
